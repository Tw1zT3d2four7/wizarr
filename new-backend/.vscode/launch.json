{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run New Backend: dev",
            "request": "launch",
            "command": "npm run dev",
            "cwd": "${workspaceFolder}"
        }
    ]
}